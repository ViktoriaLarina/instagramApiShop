import {InjectionToken} from '@angular/core';

export const BASE_URL = new InjectionToken('base_url');
export const CLIENT_ID = new InjectionToken('client_id');
export const REDIRECT_URL = new InjectionToken('redirect_url');

